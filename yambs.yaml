# =====================================
# generator=datazen
# version=3.2.3
# hash=33a62985fc163f297b76e47d2abe4f2a
# =====================================
---
project:
  name: yambs-sample

  github: {owner: &self libre-embedded}
  version: {major: 0, minor: 3, patch: 3}

dependencies:
  - github: {repo: yambs-sample2, owner: libre-embedded}
  - github: {repo: yambs-sample3, owner: libre-embedded}
    as_source: true
variants:
  clang:
    suffix: &clang_version "-19"

  debug:
    suffix: &gcc_version "-14"
  opt:
    suffix: *gcc_version

  clang-opt:
    suffix: *clang_version

includes:
  - local/yambs.yaml
