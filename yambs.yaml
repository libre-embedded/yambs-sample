# =====================================
# generator=datazen
# version=3.2.1
# hash=a1ca9f8868e3299acbaa278ecabb6f72
# =====================================
---
project:
  name: yambs-sample

  github: {owner: &self libre-embedded}
  version: {major: 0, minor: 3, patch: 1}

dependencies:
  - github: {repo: yambs-sample2, owner: libre-embedded}
  - github: {repo: yambs-sample3, owner: libre-embedded}
    as_source: true
variants:
  clang:
    suffix: &clang_version "-18"

  debug:
    suffix: &gcc_version "-13"
  opt:
    suffix: *gcc_version

  clang-opt:
    suffix: *clang_version

includes:
  - local/yambs.yaml
